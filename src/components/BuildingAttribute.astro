---
export interface Props {
  label: string
  value: string
}

const { label, value } = Astro.props
---

{
  value && (
    <div>
      <strong>{label}</strong>
      <span>{value}</span>
    </div>
  )
}
